# Klein's review — solo exploration of the app

2026-07-21. Verdict: I went in to break the mathematics and could not — every displayed number I attacked survived independent recomputation, including the 16-digit Euler product, the full f(p) table by a different algorithm, and the entire irregular-prime cast. What I found instead is one genuinely false sentence in the Q&A, one silently borrowed theorem, one circular demonstration, several latent domain gaps in the library, and a stack of order-and-prose defects. That is a good outcome for an app and a fixable list for an author. Everything below is additional to my notebook items A1–A5 / B1–B7; where I sharpened an existing item I say so.

## C. Mathematics — additional findings

**C1. Q&A item 6 is false as stated.** *Location:* Q&A tab, item 6, index.html line 818. *Observation:* "over Q no Z^r deck group exists at all — … — so an integer-valued arithmetic linking number has nowhere to live." The cyclotomic Z_p-tower is a rank-one deck group over Q; it merely ramifies at p. The app itself says this correctly twice: the whyFS box (line 210) carries the qualifiers "unramified outside S ∪ {∞}, ℓ ∉ S," and Q&A 14's fracture (4) at line 850 states "Iwasawa's genuine Z_p-tower ramifies at p itself." Q6 dropped the unramified hypothesis and turned a scoped theorem into a false absolute — and the knotted-prime card's whole story runs on the tower Q6 denies. *Verification:* source comparison of lines 210, 818, 850; two of the three cannot be read consistently with the third. *Modification:* rewrite the clause: "over Q no Z^r deck group unramified at the pᵢ exists — the fS deck group is finite (whyFS box) — so an integer linking number has nowhere unramified to live; the one Z_p-tower Q does have, the cyclotomic one, ramifies at p (fracture 4)."

**C2. Q&A item 2 silently uses the Poincaré conjecture.** *Location:* Q&A tab, item 2, line 802. *Observation:* Minkowski's π₁ét(Spec Z) = 1 is said to "select S³, a simply connected closed 3-manifold, rather than an arbitrary one." Simple connectivity selects S³ among closed 3-manifolds only via Perelman (2003); as stated, Minkowski selects a homotopy 3-sphere. This is the one uncited load-bearing theorem in a tab whose premise is name-and-year. *Verification:* grep — "Perelman"/"Poincaré conjecture" appear nowhere in index.html. *Modification:* one clause: "…a simply connected closed 3-manifold is S³ by Perelman's proof of the Poincaré conjecture (2003) — the one theorem this dictionary borrows from my side of the mirror."

**C3. The product-formula demonstration is circular at v = 2.** *Location:* Pairs tab, "The product formula" card (#hilbertTable), renderHilbertTable lines 1355–1383 (v = 2 row at 1363/1366); self-test at lines 3074–3086. *Observation:* rows v = ∞ and v ∉ {2,p,q} are hardcoded "+1"; the v = 2 factor is computed by (−1)^{((p−1)/2)((q−1)/2)} — a formula equivalent to quadratic reciprocity, the theorem being demonstrated. So "∏_v = +1 ✓" can never fail unless legendre() itself breaks, and the "closedness forces the ends to agree" story runs backwards. The third column discloses the formula, so this is honest disclosure but weak epistemics — the one spot where "computed live" and the actual content diverge. *Verification (independent local value):* my mod-8 enumeration for (3,7): z² ≡ 3x² + 7y² has no primitive solution mod 8, so (3,7)₂ = −1, agreeing with the closed form; legendre(3,7) = −1, legendre(7,3) = +1 in my own Node run. *Modification:* compute (p,q)₂ by mod-8 solvability over primitive triples (Hensel lemma cited for odd units), display that as the v = 2 row, let the ✓ compare independent value against closed form, and retarget the self-test at the independent computation.

**C4. Δ_Hopf and Δ_Bor are uncited fixtures.** *Location:* Triples poly-demo line 501; Pairs poly-demo line 285; Zeta line 738; consumed by expandDeltaBor (1100) and borDiagCoeffs/torresBorOneVar (2410–2424). *Observation:* the two most load-bearing polynomial inputs are neither computed (no Fox calculus exists in the app) nor carried with name-and-year at point of use — a breach of the house rule at the worst possible spot, since the coefficient grid, the Torres collapse, both τ ≐ Δ instances and the unipotent trace story are computed FROM them. The values are correct. *Verification:* (t−1)·Δ_Bor(t,t,t) = (t−1)⁴ = det(tI − h₁∗) on the b₁ = 4 Bennequin fiber (χ = 3 − 6 = −3), consistent. *Modification:* provenance tooltip at each of the three sites: "cited, not computed — standard Fox-calculus results (Kawauchi's tables; Borromean case Milnor 1954/Murasugi–Traldi), cross-checked live by the Torres and fibered identities on the Zeta cards."

**C5. legendre() and redeiSymbol() never check primality.** *Location:* lines 885–892 and 935–953; prose crediting the gatekeeper at lines 399 and 1825 ("redeiSymbol() rejects them"); the actual guards live in the UI (isPrimeSmall at 1295, Ladder at 2898). *Observation:* Euler's criterion silently passes Euler pseudoprimes — in the live page, legendre(2n, 341n) returns +1 (341 = 11·31); a composite q slipping into redeiSymbol would yield a silently meaningless symbol. Latent, not live: every current call site is guarded. But the visible text credits the library, not the UI. *Verification:* 2^170 mod 341 = 1 (recomputed today). *Modification:* add isPrimeSmall guards on p₁, p₂, q inside redeiSymbol and a contract comment on legendre; add one self-test "redeiSymbol rejects composite q" so the prose becomes literally true of the function.

**C6. The one-≡3 (mod 4) gloss borrows a picture whose hypothesis fails.** *Location:* Pairs tab, warn branch lines 1307–1309 and glossed banners 1338–1346; e.g. (7,13): "13 is inert in Q(√7): the Hopf pair." *Observation:* for p ≡ 3 (mod 4), Q(√p) has discriminant 4p — ramified at 2 as well as p — so the Dictionary row-2 premise ("surface touching only its own knot") silently degrades; the existing warn covers only reciprocity's 2-adic factor. Nothing asserted is false; the gloss just leans on a hypothesis the slot violates. *Verification:* disc(Q(√7)) = 28, disc(Q(√3)) = 12; symbols themselves verified correct in every branch, including swap-antisymmetry on both-≡3 pairs. *Modification:* extend the warn: "…and Q(√p) for p ≡ 3 (mod 4) is ramified at 2 as well — the 'touches only its own knot' reading belongs to the ≡ 1 slot; splitting and symmetry are what survive."

**C7. redeiSymbol misdiagnoses the q | z certificate case.** *Location:* line 951, the "conjugates disagree" throw. *Observation:* if q divides z, exactly one conjugate x ± y√p₁ vanishes mod q (both vanishing forces q | gcd — algebra: q | 2x and q | 2yr gives q | x, q | y, then q² | p₂z² gives q | z, contradicting primitivity), so the legs differ and the error text claims a well-definedness failure when the honest diagnosis is "q is not unramified in Q(√α) for this certificate." Unreachable from current call sites (all shipped certificates have z = 1). *Modification:* check vals for a zero before comparing legs; throw "certificate shares a factor with q; choose one with q ∤ z."

**C8. The 8081 verdict rides an untested code path, and is one-shot.** *Location:* Zeta tab, check8081 handler lines 2662–2677 (btn.disabled = true at 2665, never re-enabled); irregularIndicesAsync lines 2638–2647; test R5-K3 (3482–3499) pins only the synchronous twin. *Observation:* the app's only on-demand computation runs through chunked bookkeeping no test exercises; a boundary bug at line 2642 would surface only on the live demo's most-watched moment. And since the button never re-enables, "consistent on re-run" is untestable without a reload — against the recompute-on-demand house rule. *Verification:* my independent run (own Akiyama–Tanigawa mod p implementation): 8081 has no irregular indices — regular, 144 ms; same code reproduces 37:[32], 59:[44], 67:[58], 101:[68], 103:[24], 691:[12,200]. The displayed verdict is correct. *Modification:* add a test driving irregularIndicesAsync synchronously and asserting equality with irregularIndices(691) = [12,200]; in onDone, re-enable the button as "re-check 8081."

## D. Presentation — additional findings

**D1. Teaching order: Zeta consumes the Ladder one tab early.** *Location:* nav lines 173–179 versus Zeta k₁₃,₆₁ card line 2724 ("Each row below powers the Ladder tab's matrix, live"), triplesThread line 1857, Q&A line 814. *Observation:* a cold reader walking left to right — as the Dictionary's closing caption instructs — meets ρ(Frob) matrices as consumed objects a full tab before they are defined. I would teach dict / pairs / triples / **ladder** / quads / zeta / qa: the Ladder supplies the coordinate system (Frobenius as unipotent matrix, order = orbit period) that Quadruples' banner and every worked Zeta card consume. *Modification:* reorder the nav; if reordering is refused, make each forward reference self-contained: "ρ(Frob_p) ∈ N₃(F₂) (defined and powered on the Ladder tab, next)."

**D2. "101 checks" is two species presented as one number.** *Location:* runSelfTests lines 2982–3752; dialogue line 99 and Q&A 10 (line 834) present it as the proof-of-honesty beat. *Observation:* 52 computational checks and 49 "DOM" tests pinning rendered sentences. Prose pins are legitimate regression armor, but a wording edit flips them with no mathematics changing, and "101/101" implicitly claims 101 recomputations. Strongest three tests: the N₄(F₂) rule brute-forced 64/64 (line 3686); the θ′/θ″/scaled-certificate exploit family (3028/3039/3176), which pins the *necessity* of Rédei–Amano normalization; the 16-digit Euler-product pin (3644). Weakest: the circular Hilbert test (C3); pure prose pins; the three-faces tolerance 5e-3, ~25× looser than the measured 2.4e-4 truncation error. *Modification:* label the button "101/101 (52 computational, 49 rendered-text pins)"; tighten the tolerance to 1e-3 with a comment recording the measured error.

**D3. Compute and the presets disagree about the topology panel.** *Location:* Pairs — #computeLegendre handler line 1385 versus preset529/preset513 (1404–1413); #pairsStateLabel. *Observation:* presets sync the drawing (setPairsTopology); manual entry does not. Type 5 and 13: the banner announces the Hopf pair while the canvas beside it still shows "lk = 0 (unlinked)." In an app whose discipline is box-for-box mirroring, the columns silently contradict on every hand-entered pair. *Modification:* on Compute, call setPairsTopology(pq === −1) whenever the gloss is licensed; or one clause under the state label admitting the drawing is a toggle, not a mirror.

**D4. The triple is pinned to (13,61,937) and nothing says so.** *Location:* Triples tab arith column; Ladder intro line 773; redeiSymbol at 935. *Observation:* the only numeric inputs in the whole app are pInput/qInput and the Ladder's third slot. I built a fresh triple meeting every hypothesis — (5,29,109), all ≡ 1 (mod 4), pairwise +1, normalized certificate (7,2,1) for x² − 5y² − 29z² = 0 — and the engine handles it perfectly from the console: redeiSymbol(5n,29n,109n,7n,2n,1n) = +1, matching my independent computation (distinct-degree factorization of x⁴ − 14x² + 29 and x² − 29 mod 109 → f(109) = 1; conjugates 49, 74 mod 109, both squares). Machinery ahead of its cockpit, and the restriction's reason — the app verifies certificates, it does not search for them — is stated nowhere. *Modification:* (a) one sentence on Triples: "the pair (13,61) is pinned because the app ships Rédei's normalized certificate (23,6,1) as data; it verifies certificates, it does not search"; (b) optionally a power-user (p₁,p₂,q,x,y,z) input feeding redeiSymbol, which already enforces every check.

**D5. The Quadruples opener is contradicted by its own tab, and "ladder" has three referents.** *Location:* line 518 ("The papers stop at triples; the ladder does not"); also Triples #surfaceLadder (406), Ladder heading (747), Q&A 13 (846). *Observation:* Amano (Tohoku Math. J. 66, 2014) — the tab's centerpiece — is a paper about quadruples; what stops at triples is Rédei's construction. And the first standalone "the ladder" precedes every definition of any ladder; a cold reader meets three senses in tab order. *Modification:* rewrite the opener: "Rédei's classical construction stops at triples; Amano (2014) and the unipotent ladder run one more level — the same argument, one matrix size up." Qualify every other use: "the surface ladder (Triples)," "the unipotent ladder (Ladder tab)."

**D6. Two tab openings misfire.** *Location:* Triples first element (line 341); Pairs opening (219–223). *Observation:* Triples leads with a scroll pointer ("Further down this tab: Climbing the ladder…") — navigation before mathematics, referencing an undefined ladder. Pairs is the only tab with no lede at all: the reader lands straight on the two-column block. *Modification:* Triples: lead with the Borromean claim now buried in the left column's .sub ("no two linked, all three inseparable — the first invariant that survives pairwise vanishing") and demote the jump note below the banners. Pairs: one lede sentence — "One intersection count computed in both languages: lk by counting punctures, (p/q) by Euler's criterion — and one symmetry with the same proof twice."

**D7. "Powered live" powers a stand-in.** *Location:* zxBorro (line 2447) and zxBorroWorked (2740); borUnipotentTraces 2425–2431. *Observation:* the code iterates a single 4×4 Jordan block, but the app nowhere knows the actual Jordan type of h₁∗ — only the characteristic polynomial is pinned. Nothing displayed is wrong (traces and both zeta faces depend only on eigenvalues), but the phrase implies the app holds h₁∗ itself. *Modification:* "(a stand-in unipotent with the same characteristic polynomial, powered live — every quantity used depends only on the eigenvalues)."

**D8. "The four conjugates disagree this run" is mathematically unreachable.** *Location:* quad449Ord (1896–1901); rendered branches 1914–1915, 2348, 2799–2802, 2819–2822. *Observation:* for prime 449 and a fixed certificate the conjugate values are deterministic ({182, 340, 159, 317}, all Legendre −1 — recomputed) and their agreement is forced by Frobenius-stability; the branch presents a theorem as an empirical contingency. *Modification:* keep the guard, reword the fallback as an internal assertion ("they agree, as they must at a prime modulus — checked anyway").

**D9. Running the self-tests destroys the presenter's pinned tooltips.** *Location:* test R3-A1, lines 3293–3306; document-level Escape at 3302 triggers ttCloseAll(null). *Observation:* the suite scrupulously restores every other state (R4-T5, R3-M5) but closes every pinned .tt — exactly the pin a presenter set for the audience before the closing "run self-tests" beat. Verified by driver: pin, run, no .tt.tt-open remains (count unaffected, 101/101). *Modification:* snapshot .tt.tt-open before the Escape dispatch and re-open the set afterwards via ttSet.

**D10. The Ladder refuses q = 2 while the Zeta tab computes it.** *Location:* frobeniusMatrix1361 rejection at 2898 versus k1361FrobData (2682–2689), which displays f(2) = 4. *Observation:* the bare "q must be an odd prime." is correct for Euler-criterion machinery, but the app knows the answer one tab over (13 ≡ 61 ≡ 5 mod 8 → 2 inert in both quadratics → order 4, which I verified via the D₄ rotation argument); the same message also covers composite odd input. *Modification:* extend the q = 2 branch: "q = 2 needs the mod-8 criterion — its row of the ζ of k₁₃,₆₁ card computes it: f(2) = 4"; split the composite case.

**D11. The one network dependency fails silently.** *Location:* three.js from cdnjs, lines 1714–1721; onerror hides #borro3dToggle. *Observation:* offline, the toggle disappears with no explanation and the Freedman–Skora content (tooltip inside #borro3dWrap) becomes unreachable — in an otherwise fully self-contained artifact. *Modification:* inline a 30-line canvas fallback (three static ellipses with rotation needs no library), or leave the button visible-but-disabled with an explanatory title and move the theorem note outside the hidden block.

**D12. Keyboard access passes, with one structural gripe.** *Location:* tab bar lines 173–179; Zeta focus inventory. *Observation:* full keyboard pass PASSES (Enter pins tooltips, Esc closes, the ζ slider recomputes on ArrowRight — all driver-verified), but the tab bar is 7 separate stops re-traversed on every switch and Zeta has 51 stops, 34 of them tooltip spans. *Modification:* role="tablist" with roving tabindex (one stop, arrows move) plus a skip-to-panel link; additive, breaks nothing visual.

**D13. Two small hygiene items.** (a) *Crossing-sign convention:* the y-down/left-handed frame lives only in the comment at line 1642; the app is immune today because only zero totals are displayed from diagrams, but that design rule is invisible — promote it to one visible sentence on Pairs. (b) *Citation:* line 2286, "Corsman 2007; Stevenhagen" — the only bare citation in an otherwise accurate apparatus; complete it (Stevenhagen, "Rédei reciprocity, governing fields, and negative Pell," Math. Proc. Camb. Phil. Soc., 2018 — verify volume before pinning).

## E. Amendments to existing notes

**Amends A5 (test-suite hygiene) — a second state-fragile test, and it compounds.** The gallery-spine test at line 3640 asserts at run time that #zxHopf is closed: open that one card (real click or .open) and the suite reads 100/101 with a false ✘; with a Q&A item also open, 99/101. Driver-reproduced on fresh loads, with a control (#zxSqrt5 open → 101/101) isolating the culprit clause. Fix alongside A5 the same way: assert against the ZX_AT_LOAD snapshot the neighboring R6-G2 test already uses, or drop the open-state clause — the content assertions in the same test are already state-independent.

**Amends A3 (five-component rung) — half of it computes today.** Using only the app's own functions in the live page: the Magnus coefficient of X₁X₂X₃X₄ in [[[x₁,x₂],x₃],x₄] is 1, and matOrderF2 on the 5×5 corner of N₅(F₂) returns 2 — so μ̄(12345) = 1, ρ(K₅) has order 2, and the 1024-sheet cover gives 512 doubled loops, factor (1 − t²)⁻⁵¹². This *sharpens* the thesis-problem close: one column computes live, the other is an honest blank waiting on the quintuple independence theorem. Do not soften it — stage it.

**Amends B1 (trim framing) — three measured density targets with rewrites.** (i) threadDict, line 191: 153 words / 5 sentences, first sentence 91 words with 22 clause marks, on the app's first screen; one-sentence core proposed: "Two zetas (ζ_K ↔ Lefschetz) and two polynomials (Δ_L ↔ f_S), each polynomial the zeta of its own cover up to units (Milnor–Turaev; the Main Conjecture) — each tab computes one more piece, and the Zeta tab cashes out." (ii) Milnor-convention note, line 609: 77 words, one sentence, 25 clause marks, three convention switches; compress to the ζ_Lef ≐ Δ_K/(1 − t) identity and push the fine print into the existing tooltip at 605. (iii) triplesThread (injected 1856–1858): 183 words / 3 sentences, nested parentheticals — the box a presenter most wants to read aloud and cannot; lead with the orbit count, move the q = 107 caveat to its own small-note.

**Amends B3 (long-scroll tabs) — the cover towers are the weak discovery path.** The Pairs mini-tower (lines 294–335) sits at the tab's bottom unannounced; the Triples jump note advertises only the surface ladder, leaving #tripleCoverHasse (≈1.8 screens down, measured) to full-scroll luck. Narrower fix than the sticky mini-nav: one pointing clause on Pairs and extend the Triples jump line to "surface ladder · cover tower ↔ field tower."

## F. What I would build next

1. **The ideal-sum face of the degree-8 zeta card.** The gallery's own standard — one number, three faces — is met only at degree 2; the existing Frobenius data already determines a(p^k) = C(k/f + g − 1, g − 1) at unramified primes, so the Dirichlet face is a sieve away and directly comparable to the shipped product 1.0673950922954751 (which I matched to all 16 digits). Add a cited-not-computed sentence that the third face upgrades to Artin L-functions because D₄ is nonabelian — itself a teachable point the card now skips.
2. **The half-computed five-component card.** As in the A3 amendment: μ̄(12345) = 1 and the corner order 2 computed live, the arithmetic cell an honest blank naming the missing theorem. It converts the closing problem from prose to a visible half-built rung.
3. **An honest 2-adic Hilbert symbol.** The mod-8 solvability computation from C3, displayed as the v = 2 row, turns the app's only circular demonstration into a genuine two-sided check of reciprocity. Twenty lines, and the product formula finally has something to lose.
4. **A certificate cockpit.** The (p₁,p₂,q,x,y,z) input of D4, feeding redeiSymbol with the C5 primality guards added first. The engine's rejection taxonomy is already exemplary; give users a door to it, seeded with (5,29,109; 7,2,1).
5. **A no-network ellipse fallback.** Inline canvas projection for the Freedman–Skora view so the artifact's self-containment claim is true offline. Three rotating ellipses do not need a CDN.

The mathematics held; the epistemics leaked in exactly four places (C3, C4, C5, D7), and those are the ones I would fix before anyone sharper than me gets a projector and an hour. — J.K.
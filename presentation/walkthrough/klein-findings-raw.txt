- Dictionary table, row 1: the source ships literal ASCII subscripts -- 'K_r' and 'p_r' -- in cells whose neighbours (K1, p1) are properly typeset with <sub> tags. It reads as leaked source in an otherwise carefully set table (index.html line 196).
- The whyFS why-box (d5) is written almost entirely in ASCII math: 'Z_l[[u_1,...,u_r]]', 'v_l(p_i - 1)', '(1+u_i)^(l^e_i)', sitting immediately below a properly typeset table. It is the most technical paragraph on the tab and the worst set (index.html line 211).
- The Artin-Verdier justification -- trace isomorphism, G_m as orientation sheaf, the real-place 2-torsion correction, and both citations -- lives entirely inside a hover tooltip. The visible page asserts 'behaves 3-dimensionally' with only a parenthesis, so an unaided reader sees an unsupported premise for the whole dictionary.
- The Dictionary thread box cites 'Milnor-Turaev' with two names but 'the Main Conjecture' with no name and no year, violating the app's own stated name-and-year-at-point-of-use rule. Mazur-Wiles 1984 and Greither 1992 (the l=2 case, which is the only case the app ever instantiates) appear only on the Zeta and Q&A tabs.
- The Dictionary table puts lk in Z opposite the Legendre symbol in {+-1} with no marker of the information loss; the concession that the symbol is only a mod-2 linking number is buried in a collapsed why-box on the Pairs tab. The p10 lift diagram, by contrast, correctly says 'lk even' rather than 'lk = 0'.
- Pairs whySeifert why-box states the surface-independence argument as gluing two Seifert surfaces 'along K1 into a closed surface' -- the orientation reversal (glue Sigma to -Sigma') is omitted, and it is the entire content of the argument.
- The arc figure (p6) paints its labels under the Sigma1 ellipse: 'K2 pierces Sigma1 here' and the 'Sigma1 n Sigma2' label are both struck through by the blue curve. These are the two labels naming the two ends of the global object the proof turns on.
- The arc figure draws both Seifert surfaces as flat disks, available only because both components are unknotted; nothing on the figure says the picture is the genus-zero case while the argument (transversality plus signed boundary zero) does not need disks.
- The Hilbert product-formula table presents five rows as a computation, but the row 'v not in {2,p,q,inf} = +1, both are local units' is hard-coded -- an infinite family of places asserted from a (true, standard) theorem, wearing the same typography as the genuinely enumerated v=2 row and the Euler-criterion rows.
- Entering (3,7) leaves the topology canvas showing 'lk = +-1 (Hopf link)' from the previous preset, because the Compute handler only re-syncs setPairsTopology when at least one prime is 1 mod 4 (index.html line 1838). The stale picture sits beside a banner explicitly withholding the linking gloss, so a deliberate refusal renders identically to a forgotten update.
- The both-primes-3-mod-4 warning (line 1750) omits the caveat that Q(sqrt p) then has discriminant 4p and is ramified at 2 -- a caveat the mixed-parity warning (line 1747) states explicitly. The case where it applies to both fields is the case that drops it.
- The v=2 row's closed-form cross-check renders its exponent ((p-1)/2)*((q-1)/2) as a tiny sub-baseline run that wraps mid-formula, in the one cell whose whole purpose is to be independently readable.
- The 'Lift apart' bounce animation in the Hopf state prints 'Bounced back -- a genuine crossing survives every isotopy', which is a mnemonic dressed as evidence: the return is a property of the easing function, not of the link.
- The lift diagram (p10) renders 'X-tilde' with the combining accent displaced in three separate labels, on the one figure whose job is to distinguish the cover from the base.
- The strongest available point on the Pairs tab -- that choosing both primes 3 mod 4 makes the 2-adic Hilbert factor visible as a term in the product formula rather than an exception to it, so the 1-mod-4 convention is revealed as a choice to work where one local term vanishes -- is never stated in one place; the reader must assemble it from the warn line, the product note, and the Dictionary's orientability remark.
- Q and A tab computes nothing. It is the tab whose entire subject is what is computed live versus cited, and it contains no live number at all -- fourteen prose answers plus jump buttons. Every claim's support is one click away and none is visible on the tab itself.
- Q and A tab has 20 jump buttons across exactly six destinations (dict, pairs, triples, ladder, quads, zeta) and none to the Experimental tab. The skeptic's index routes the reader away from the frontier tab -- notably question 13 discusses the five-component frontier and does not link the tab that explores it.
- Q and A questions are unnumbered despite the intro announcing 'Fourteen questions', there is no expand-all control, and reading the tab straight through requires fourteen separate clicks.
- The term f_S is used twice in the mod-2 answer (Q6) and again in fracture (4), and is never defined anywhere on the Q and A tab.
- Fracture (4) -- that over Q no Z^r-cover unramified outside S exists, so the two polynomials are forced over different rings -- is the only theorem-grade item in the breakage list and it is buried as item four of five in an undifferentiated run-on paragraph, ranked by app position rather than weight.
- Experimental tab: the .honest CSS class (0.85rem, muted colour, italic, centred) is applied to the frontier banner, the two-cracks box and the ell=3 gap list -- the three longest and most load-bearing paragraphs on the tab are its three least legible blocks. Systematic, since the class is shared.
- The trefoil table computes #H1(M_n) = prod |Delta(zeta)| -- the formula for the n-fold cyclic BRANCHED cover -- but the word 'branched' never appears on the tab (it occurs exactly once in the whole file, in an unrelated Dictionary tooltip), and M_n is never defined. A reader taking 'cyclic covers' literally computes the wrong object.
- The trefoil cover orders 3, 4, 3, 1, infinite are exactly the Brieskorn spheres Sigma(2,3,n) -- L(3,1), Sigma(2,3,3), Sigma(2,3,4), the Poincare homology sphere at n=5, and the Nil case at n=6. The app prints them as bare integers and never names them, forfeiting the strongest topological payoff on the tab.
- The knot-column arithmetic match is exhibited only in the degenerate case lambda = mu = nu = 0 (v_2 identically zero because the orders 3 and 3 are odd). Agreement by mutual vanishing, presented adjacent to the Iwasawa asymptotic without that caveat in the display itself.
- The single most important conceptual point of the tab -- that gamma is analogous to phi_* and NOT to Frobenius, which the app's own tooltip calls 'the commonest error in reading the dictionary' -- is hidden behind a hover tooltip rather than stated in the body text.
- The x2 two-column layout is badly unbalanced: the left column runs a table plus a long paragraph while the right column ends roughly halfway, leaving a large empty region beside the tab's most delicate prose.
- 'depth-four' is used twice on the Experimental tab and defined neither time, while 'depth two' IS defined in the adjacent column -- inviting the reading that depth-four means four link components, which it does not. The object it names is called 'the one machine-checked instance' and 'a governing bit' elsewhere on the same screen: three vocabularies for one thread.
- The pivot box (ramified generator gives knot invariants, unramified Frobenius gives link invariants and Artin L-functions) is arguably the sharpest idea in the whole app, yet it sits on a tab banner-labelled 'nothing here is load-bearing'. The banner measures epistemic status but reads as importance, quarantining the app's best structural distinction.
- The d-bit box never explains why the governing bit takes values in {1, 3} rather than {0, 1} or a sign, and 'a Stark rank' appears as a three-word parenthetical standing in for the whole theory.
- The (13,17) witness scan declares 'every one of the 10 normalized witnesses with z <= 12' but the code also bounds |y| <= 120, which is not stated -- an exhaustiveness claim over a search box only half described, on the tab whose subject is labelling what has been checked.
- The (13,17,53) card renders the certificate as '(-15, 4, 1)' with an ASCII hyphen while the symbol value four lines below uses the typographic minus, because the certificate array is joined raw rather than passed through the app's formatter.
- The 'Chebotarev share per class at the centre' table row reads 1/2 each versus 1/3 each, which is conditional on Frobenius landing in the centre; the Ladder tab and the Q and A tab quote 1/8 densities in the whole group. The word 'conditional' does not appear, so the denominators appear to contradict.
- Terminology drift on one screen: 'the d-defect' (cubic table row), 'a governing bit d{p,q}' (d-bit box), 'the machine-checked depth-four case' (cracks box) all refer to the same thread.
- The worked cubic reciprocity instance is degenerate: chi_{pi_7}(pi_61) = chi_{pi_61}(pi_7) = 1, i.e. 1 = 1 with a green tick. An implementation returning the constant 1 would produce an identical card; the only non-vacuous evidence is the census one paragraph below.
- The mod-3 topology table lists lk = 0,1,2,3,4 -- five rows for three dial positions, with rows 3 and 4 duplicating rows 0 and 1 -- three sentences after the prose says the dial has three positions. The periodicity demonstration is unlabelled.
- The census reports 171/171 reciprocity as a computed result without noting that cubic reciprocity is a theorem (Eisenstein, cited elsewhere on the card), so that check tests the implementation rather than the mathematics.
- The census prints the value distribution 61:57:53 as 35.7 / 33.3 / 31.0 percent to one decimal with no error bar. At n = 171 the standard deviation is about 6.2, so the deviations are roughly 0.65 sd -- consistent with equidistribution and equally consistent with much else. The precision displayed exceeds what the sample supports, and the corresponding self-test pins 61:57:53 as an exact expected constant, making it a regression pin rather than an equidistribution check.
- SERIOUS: the ell=3 gap list asserts that Q(sqrt(-3)) is 'imaginary quadratic: zero real embeddings (computed at right)', but nothing in the right-hand column computes a signature -- that column computes primary associates, cubic residue symbols and reciprocity only. A false 'computed' label on the one tab whose entire discipline is distinguishing computed from cited.
- The Experimental tab contains zero jump buttons, so it is a navigational dead end -- no route back into the tabs whose claims it reframes.
- The 52 rendered-text pins can only detect drift between code and prose; they cannot detect prose and code being wrong together, and a misattributed theorem or citation would leave the suite fully green.
- The self-test panel renders 114 flat monospace lines with no grouping by tab and no jump-to-failure; the button reports only the passing count plus 'see failures', so the suite is least usable at the only moment it matters.
- Quadruples canvas: the K4 label is drawn in blue at fixed canvas coordinates directly on top of the blue strand, making it hard to read (visible in q2-b4).
- Quadruples cover tower: the SVG edge label '2 sheets (gamma_12 unwinds)' refers to the Triples tab's curve Sigma_1 ∩ Sigma_2, which is never defined on the Quadruples tab; it is 9px grey drawn text with no tooltip, unlike every other term on the tab.
- Quadruples field tower: only the left branch carries a generator label ('deg 2, sqrt(alpha)'); the right branch k_{101,8081} over Q(sqrt101, sqrt8081) is unlabelled even though its Redei element 1009 + 100*sqrt(101) was computed in step 2 on the same screen.
- Quadruples field tower note: the compositum degree argument 8*8/2 = 32 silently uses the fact that both octics are Galois over Q; the hypothesis is never stated.
- Quadruples step 3: total positivity is checked in floating point, and the smallest embedding of theta is 0.428 — safe at this size but not certified; a theta engineered near zero would not be caught. The 2-adic normalization is cited, not computed.
- Quadruples step 5 renders its Legendre symbols with an ASCII hyphen ('{-1, -1, -1, -1}') while the verdict banner immediately below uses a typographic minus, so the two look inconsistent.
- Quadruples pairwise note computes only 4 of the 6 linking numbers from signed crossings; the other two pairs are handled by asserting K1 ∩ K3 = K1 ∩ K4 = empty. Honest and eye-checkable, but easy to skim past.
- Quadruples: the drawn diagram and Milnor's model B_4 are two different objects on one screen (mu-bar is computed only for the model), and the honesty note saying so sits at the bottom of the column, well below the figure.
- Zeta tab: the 'which zeta is which' paragraph — the disclaimer that the three zetas are related by analogy, not equality — is set in the smallest, muted type on the page, directly beneath a bolded sentence that overclaims.
- Zeta three-faces layout: the Dedekind column ends roughly a third of the way down while the Lefschetz column runs full height, so the page selling symmetry looks visibly asymmetric.
- Zeta capstone uses f_S with no definition or link on that tab (it is defined four tabs back on the Dictionary tab), while the same page later disambiguates f_Lambda from the residue degree f — three f's, two of them disambiguated.
- Zeta gallery cards are all default-closed, so the tab's densest and best material is behind a click, and the row heads are styled quieter than the intro paragraphs.
- Q(sqrt5) card prints eight decimal places for three quantities that are three different truncations of the same limit (ideals of norm <= X, complete local factors for p <= X, and independently truncated zeta and L), inviting a comparison the truncations cannot support.
- k_{13,61} card: the 'ideal-sum face agrees with the product face' check is engineered — restricting to 200-smooth norms makes the two faces the same truncation by construction — and the admission lives only in a tooltip on the word 'smooth'.
- k_{13,61} table is too narrow for six columns; the p = 2 row wraps its pairwise-level cell into five lines. The orbit-picture column duplicates information already in f and the local factor.
- Figure-eight card renders two label strings as raw ASCII TeX — '∏_d (1 − t^d)^{−o_d}' with literal braces and carets, and 'ζ_h' with an underscore — beside properly typeset lines, at the visual center of the card carrying the tab's punchline.
- Knotted-primes card: 8081, one of Amano's four primes, is missing from the judged cast and is only reachable via a small grey 'check 8081' button below the table (its Bernoulli run is too slow for page load).
- Notation collisions are flagged but never resolved: chi (Euler characteristic vs Dirichlet character), gamma (periodic orbits vs the Triples curve), lambda (dilatation vs Iwasawa invariant).
- Presentation-matrix pair: the left matrix is fully computed while the right is schematic (size and determinant ideal cited, entries unknown), yet the two-column layout presents them as a matched pair; and p = 37 is used rather than 101 without the reason being given.
- Systemic: across both tabs the supporting evidence is repeatedly one click or one hover away — tooltips carrying the load-bearing caveats, buttons carrying the missing data, jump links carrying the cross-tab proof — and the click is rarely visible.
- Triples lede: the canvas draws three ROUND circles while the app's own Freedman-Skora citation (that round circles cannot realize the Borromean rings) is buried in a tooltip inside the collapsed 3D panel -- the defence sits two clicks from the claim it defends.
- Borromean canvas: the K1 label is clipped by the canvas top edge (labelY = cy - r - 8 = 5 with a 13px font), so the glyph is cut in half.
- Triples step columns: the two four-step sequences are independent flows in a two-column grid with no row pairing, so the left step 2 is ~5x the height of the right step 2 and by the bottom the left step 4 faces blank space -- a 'mirror' whose halves slide apart.
- The link's defining datum is a fixture: the longitude word l_3 = [x1,x2] is taken from Milnor's Wirtinger computation rather than recomputed from the drawn diagram, so the diagram computes only the VANISHING (lk = 0) while the non-vanishing invariant is typed in. The app states this in step 2 but the tab lede still says 'every number computed live'.
- Verdict banners assert mu-bar(123) = 1 opposite [13,61,937] = -1 as a match; the value-group asymmetry (Z-valued vs {+-1}, i.e. mod-2) and the orientation/ordering convention are only in a '(sign?)' tooltip and on the Q&A tab, not on the banner.
- The cover/field tower caption claims 'the lattice of intermediate covers mirrors the subgroup lattice of the deck group', but the drawing shows 5 nodes where D_4 has 10 subgroups -- the third quadratic subfield Q(sqrt(793)) and the corresponding diagonal double cover are absent.
- The towers promise 'box for box' twice but have different viewBox heights (216 vs 190) with no shared baseline, so the bottom boxes (Q vs X = S^3 minus (K1 union K2)) are visibly misaligned.
- The tower captions' 4-loops and 4-primes numbers both come from the same matrix-order subroutine; the genuine independence lives only in the corner bit (Magnus coefficient vs Legendre symbol of alpha mod 937), which the app never says explicitly.
- The definition of mu-bar(123) -- the invariant the entire tab computes -- is collapsed behind a small grey 'why' toggle rather than shown by default.
- No Seifert surface is ever constructed in code anywhere in the app: lk comes from signed crossings, mu-bar from Magnus. The surface story is illustrative only, and this is admitted only in scattered figure captions rather than once, prominently.
- CSS bug: .rung-a is display:flex with flex-direction:column, so every child element becomes its own flex item on its own line. Every bolded value in all three arithmetic rails is torn onto a separate line, orphaning the surrounding prose ('(13/61) =' / '+1' / ', computed live by Euler's criterion').
- The certificate cockpit is seeded with (5,29,109; 7,2,1), which returns [5,29,109] = +1 -- the SPLIT, unlinked case. The one bring-your-own example a visitor is handed demonstrates definedness, not the triple linking the tab is about; the only -1 in the app is the pinned (13,61,937).
- The cockpit verifies but never searches for certificates, so a user cannot produce a new linked triple without supplying a normalized (x,y,z) themselves.
- The one COMPUTED figure (gamma) shows a segment, not the closed curve gamma -- it is the pre-tubing picture, and the tubing that closes it is never computed. Additionally the oblique projection makes the two ellipses read as nested rather than as two unlinked circles in general position, and K3 is absent.
- Rung 3 figure: the label 'surface spanning gamma (drawn flat)' is struck through by the purple ellipse arc, and 'the one pass through the surface:' collides with the orange K3 curve -- both are fixed SVG coordinates never checked against the drawn curves.
- Rung 3 figure hardcodes the text 'lk(gamma, K3) = 1' inside the SVG -- a number that is drawn, not computed -- on a tab whose navigation line says 'every number computed live' and whose ladder intro says 'every number on the ladder is computed live'. The app needs a third category: computed / cited / drawn.
- gamma's orientation is never explained; lk(gamma, K3) requires it, and it comes from the intersection orientation of Sigma_1 and Sigma_2. The sign discussion covers only component ordering and mirror words.
- The Delta_Bor tooltip calls the coefficient grid and the Torres collapse 'two independent cross-checks', but the coefficient-vs-mu-bar agreement is not independent: Delta_Bor is cited to Milnor 1954 and the model word [x1,x2] is Milnor's Wirtinger computation, so both descend from the same source. It tests the code, not the mathematics.
- Ladder intro does not say whose linking lives in the unipotent group, nor names the representation rho (only the Triples cover caption does), and uses 'characters' as an undefined term on this tab.
- Ladder matrix cards: N_3's corner is labelled r (arithmetic/Redei) while N_4's corner is labelled mu-bar (topological) -- two alphabets one card apart on the tab whose thesis is that the two sides are one object.
- The .matrices row is flex with align-items:center, so the three cards of different heights centre on a common midline; the superdiagonals and corners do not line up across the arrows the reader is asked to follow.
- The Frobenius section's two-tier domain caveat (corner defined for all odd unramified q; symmetric Redei symbol needs q = 1 mod 4) is correct and important but buried mid-paragraph in a ~250-word unbroken block placed above the buttons; a visitor pressing q = 107 sees a matrix and 'splits completely' with the caveat only in a parenthetical.
- The congruence q = 1 mod 4 has no independent topological counterpart in the app -- 'orientability' is offered as analogy only -- so the mirror is asymmetric precisely at the point where the arithmetic object is called a linking number.
- The Chebotarev tally has no error bars or standard deviation anywhere; the caption's claim is the verb 'hugs'. A reader cannot tell whether 0.110 against 0.125 is agreement (it is ~0.9 sigma) without computing sqrt(427 * 1/8 * 7/8) themselves. The apparent 'two singleton classes low, three doubleton classes high' pattern is forced by the fixed total (9.75 either way) and the app does not say so.
- The Ladder tab's closing caption strip writes Gal(k_13,61/Q) = D_4 SUBSET N_3(F_2) where the tab's own card asserts N_3(F_2) = D_4 (isomorphic) -- a vacuous or misleading inclusion sign on the last line anyone reads.